
When [[Create notes from cards]], the `Note folder` setting determines the folder in which new notes are created. If it is left blank, new notes are created in the same folder as the current Kanban board.
